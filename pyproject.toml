[project]
name = "career-vault-resume"
version = "0.1.0"
description = "Local-first career memory vault skill for resume and agent identity workflows."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
