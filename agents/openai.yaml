interface:
  display_name: "Career Vault Resume"
  short_description: "Local career memory and resume context"
  default_prompt: "Use $career-vault-resume to build or update my local career vault from resumes, notes, links, and job descriptions."

policy:
  allow_implicit_invocation: true
